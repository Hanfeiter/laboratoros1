#ifndef FACTORIAL_H
#define FACTORIAL_H

// Обычная рекурсивная функция
long long factorial_recursive(int n);

// Итеративная функция (будет лучше оптимизироваться)
long long factorial_iterative(int n);

#endif